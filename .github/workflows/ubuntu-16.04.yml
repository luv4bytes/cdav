name: ubuntu-16.04

on:
  push:
    branches:
      - main
    paths-ignore:
      - ".github/**"
      - "concept/**" 

jobs:
  make-master:

    runs-on: ubuntu-16.04

    steps:
      - uses: actions/checkout@master
      - name: Run Makefile
        run: |
          mkdir build
          cd build
          cmake ..
          make
