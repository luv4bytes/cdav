name: make-master

on:
  push:
    branches:
      - main

jobs:
  make-master:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master
      - name: Run Makefile Dependencies
        run: sudo make deps
      - name: Run Makefile
        run: make
